import FormAndTable from '@/components/home-page/FormAndTable';
import { useTheme } from 'next-themes';

const Page = () => {
    return (
        <div>
            <FormAndTable />
        </div>
    )
};

export default Page;
